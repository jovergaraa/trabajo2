import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyecto_inicial',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
